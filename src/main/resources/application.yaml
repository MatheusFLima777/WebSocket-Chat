server:
  port: ${PORT:8080}

spring:
  data:
    redis:
      url: ${REDIS_URL}
    mongodb:
      uri: ${MONG_DB_URI}
      database: ${MONGODB_DATABASE:chatDb}

  cache:
    type: redis
    redis:
      time-to-live: 600000

app:
  auth:
    jwks-url: ${JWKS_URL}